info:
  name: Get Packages
  type: http

http:
  method: GET
  url: http://127.0.0.1:5000/packages/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
